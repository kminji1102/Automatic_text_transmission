# Epic 1 회고 — 프로젝트 초기화 및 설정 모듈

**날짜:** 2026-05-28  
**담당자:** minji  
**Epic:** Epic 1 — 프로젝트 초기화 및 설정 모듈  

---

## Epic 요약

| 항목 | 내용 |
|---|---|
| 완료 스토리 | 3/3 (1.1 골격, 1.2 config, 1.3 run.bat+README) |
| 테스트 | 14/14 PASS |
| validate-quick.ps1 | PASS |
| 코드 리뷰 REJECTED | 0 (패치 3건 적용 후) |

---

## AC 체크리스트 결과

### config.py
- ✅ `resolve_cohort("SFAC 33기")` → `"33기"` (contains 방식)
- ✅ `resolve_cohort("35기 지원자")` → `None`
- ✅ `resolve_cohort("")` / `None` → `None`
- ✅ `validate_config()`: 키 누락 시 `ValueError` + 키 이름 포함

### 프로젝트 골격
- ✅ `main.py` (stub — Epic 2에서 구현)
- ✅ `config.py` (구현 완료)
- ⬜ `notion_client.py` — Epic 2 범위
- ⬜ `ppurio_client.py` — Epic 2 범위
- ⬜ `logger.py` — Epic 2 범위
- ✅ `tests/__init__.py`
- ✅ `logs/.gitkeep`
- ✅ `.env.example`: `NOTION_TOKEN`, `PPURIO_ID`, `PPURIO_KEY` 3개 키 + 주석

### 의존성
- ✅ `requirements.txt`: `requests==2.31.0`, `python-dotenv==1.0.1`
- ✅ `requirements-dev.txt`: 위 두 개 + `pytest==8.1.1`

### run.bat / README
- ✅ `run.bat`: `venv\Scripts\python.exe main.py` + `pause` (무조건부)
- ✅ README: 신규 기수 추가, 발신번호 변경, 중복 발송 방지 포함

---

## 잘 된 점

1. **테스트 커버리지 완성도** — 14개 케이스, `None`/빈 문자열 엣지케이스까지 구현
2. **contains 방식 기수 매핑 설계** — 딕셔너리 한 줄 추가로 신규 기수 대응하는 확장 가능한 구조
3. **보안 원칙 준수** — python-dotenv로 .env 로드, API 키 하드코딩 없음

---

## 도전 과제 / 반복 패턴

### 패턴 1: Optional 파라미터 타입 힌트 누락
- `resolve_cohort(raw_cohort: str)` — 실제로 `None`을 받지만 선언에 `str`만 명시
- 리뷰에서 발견, `str | None`으로 수정

### 패턴 2: run.bat pause 방식 오해
- `if errorlevel 1 pause` (조건부)로 구현 → AC-8은 무조건부 `pause` 의도
- 명세 모호성 + 구현 시 기본값 선택 오류

### 패턴 3: 테스트 환경변수 격리 불완전
- `test_raises_on_missing_ppurio_id`에서 `PPURIO_KEY` 격리 누락
- 테스트는 통과했지만 격리가 불완전 → `monkeypatch.setenv("PPURIO_KEY", "key")` 추가

---

## 하네스 개선 제안

### validate-quick.ps1 추가 검증 (Epic 2 시작 전)
- `notion_client.py`, `ppurio_client.py`, `logger.py` 파일 존재 여부 확인 항목 추가 예정

### CLAUDE.md 개선
- run.bat: `pause`는 무조건부임을 명시 (`if errorlevel` 사용 금지)
- 타입 힌트: `None`을 받을 수 있으면 반드시 `str | None` 명시

---

## Epic 2 준비

**의존성:** Epic 2 모든 모듈이 `config.py`를 import
- `KST`, `COHORT_SENDER_MAP`, `SMS_MESSAGE`, `API_TIMEOUT`, `LOG_DIR` 사용
- `validate_config()`, `resolve_cohort()` 함수 활용

**구현 순서:** `logger.py` → `notion_client.py` → `ppurio_client.py` → `main.py`

**주요 주의사항:**
- 뿌리오 API 엔드포인트 플레이스홀더 유지 (공식 문서 확인 전)
- KST 기준 날짜 처리 (`timezone(timedelta(hours=9))`)
- UTF-8-BOM 인코딩 (`utf-8-sig`)

---

## 커밋 이력

```
6199921 fix: apply Epic 1 code review patches
656c479 chore: make quick validation script powershell compatible
ecc62ca feat: add run.bat and README
49fc9ab feat: add project skeleton and dependency files
28f97b6 feat: implement config module with resolve_cohort
117a952 docs: add phase A/B/C prompt files
```
